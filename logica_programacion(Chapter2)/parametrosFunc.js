/*
function suma(num1,num2) {
    let res = num1 + num2;
    document.write(res);
    return res;
}

suma(20,20);
    let resultado = suma(20);

document.write(resultado);
*/

// Haciendo función saludar

function saludar(nombre){
    let frase = `Hola ${nombre}, ¿Cómo estás?`;
    frase = 3;
    document.write(frase);
}
//let nombre = "Eduardo"; 

saludar("Eduardo");